# Frontend-to-backend routing
# For local development, keep this pointed at your local FastAPI server.
VITE_API_BASE=http://localhost:8000

# Optional websocket override; defaults to VITE_API_BASE + /ws
# VITE_WS_URL=ws://localhost:8000/ws
